// Re-export kept for the adapters test suite; the real implementation moved
// to `../src/testing.ts` so @cuekit/mcp tests can share it via the package
// export.
export { FakeTmuxRunner } from "../src/testing.ts";
